# Python byproducts (from tools/ and the .claude hooks)
__pycache__/
*.py[cod]
*.egg-info/

# Editor / OS noise
.DS_Store
*.swp

# Per-repo Claude Code setups (generated output, installed into target repos)
meta-env-setup/claude-setups/

# Generated courses (runtime staging — course-factory/DESIGN.md; moved out of the repo by the
# author after delivery, never tracked here)
course-factory/courses/

# Never commit personal documents or real secrets
*_CV.pdf
.env
!.env.example
