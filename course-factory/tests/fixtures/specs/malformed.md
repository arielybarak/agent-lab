this is not a course spec at all — no headings, no sections, just prose.

It exercises the "missing or malformed COURSE_SPEC.md" edge case: the pipeline must halt at intake
with a clear reason and write no partial course folder, rather than guessing at structure that
isn't there.
