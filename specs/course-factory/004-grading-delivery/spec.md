# Feature Specification: Grading & Delivery — The Rubric, Course-Evaluator, Report, Harvest & Comparison

**Feature Branch**: `004-grading-delivery`

**Created**: 2026-07-08

**Status**: Draft

**Input**: User description: "Build the Grading & Delivery layer of the course factory — spec #4 of four, which owns the ONLY definition of quality and everything that quality feeds. This owns: (1) the one-rubric-two-layers definition — a generic core (correctness, grounding, flow, coverage, practicality) plus optional topic add-ons the course spec can request (e.g. a fabricated-capacity-numbers check) — as the single quality contract, with NO rival rubric anywhere; the same rubric is the lesson gate that spec 003 consumes and the engine the course-evaluator runs on. (2) The course-evaluator — the course-level grader that applies the rubric to a course, spot-checks citation traceability (a claim maps to a real SOURCES.md entry — it verifies tracing, NOT truth) while honoring 002's mentor-added / thin-grounding tags, and emits a graded scorecard (rubric scores, wins, cleanups, verdict). (3) COURSE_REPORT.md — the final graded scorecard delivered to the user, generated by the /course-report command inherited from the template, certifying the course to the user; distinct from FEEDBACK.md. (4) The FEEDBACK.md → insights/ harvest — pumping each course's FEEDBACK.md up into the cross-course insights/ digest (plus user-triggered capture) so the factory compounds and gets better with each course; intake/syllabus/drafting read that digest. (5) comparison/ — analyze well-made external GitHub courses against ours to produce (a) proposed REVISIONS to the one template rubric (it never keeps a rival rubric) and (b) a per-course report feeding the /improve-course backlog. Consumes 001's delivery terminal-state/folder contract (001 invokes /course-report at delivery; 004 owns the report's grading and contents) and 002's SOURCES.md + mentor-added tags (read for traceability, not populated). Explicitly OUT of scope: pipeline orchestration / gates / state machine / resume / the delivery terminal-state mechanics (spec 001); research + SOURCES.md population + syllabus composition (spec 002); skeleton & lesson authoring + the parallel author-critic worker pool + the per-lesson author-blind evaluators + the fake-student check (spec 003) — 003 consumes 004's rubric as its gate but owns the per-lesson application. Honor the settled seam decisions: 001 FR-023 forward-diff-only (a course-level gap surfaces as a forward diff / feeds the improve-course backlog, never reopens a passed phase); 002's compose-and-flag thin-grounding policy (the evaluator accepts mentor-added marked claims and preserves their flags, never failing them merely for lacking an [Sn])."

## Overview

This is **spec #4 of four subject-specs**. It owns the **only definition of quality** in the factory
and everything that definition feeds. Its center is a **single rubric** (one-rubric-two-layers); its
outputs are the **graded scorecard** the course-evaluator emits, the delivered **`COURSE_REPORT.md`**
that reports a course's graded quality to the user, the **`FEEDBACK.md` → `insights/` harvest** that makes the
factory compound, and **`comparison/`**, which proposes revisions to that one rubric from external
courses.

It sits at both ends of the pipeline: the rubric is the **gate that spec 003 consumes** during lesson
authoring, and the course-evaluator + report + harvest run at (and after) **delivery**, which spec
001 orchestrates. 004 defines the rubric and owns the course-level grading and the report contents;
001 owns *when* delivery happens and the folder contract, 002 owns the grounding store the evaluator
reads, and 003 owns the *per-lesson application* of the rubric.

### Plannable increments — this spec bundles four loosely-coupled subsystems

This spec.md covers four subjects at very different points on the critical path: **US1** (the rubric
+ gate contract) is **003's hard prerequisite** — nothing in the lesson phase can run without it.
**US2** (course-evaluator + `COURSE_REPORT.md`) gates delivery. **US3** (harvest) and **US4**
(`comparison/`) are **post-MVP** by this spec's own priority ordering (P3/P4) and by the README's
build order, which already builds 004 in two separated chunks: *rubric core* (before 003) and
*delivery/harvest/comparison* (last).

**US1 is independently plannable and taskable, ahead of and without waiting on US2–US4.** A
`/speckit-plan` + `/speckit-tasks` pass scoped to US1 alone is sufficient to unblock 003; US2's
delivery-time grading, US3's harvest, and US4's `comparison/` can be planned and built later, on their
own schedule, without re-planning US1. Treat this spec.md as pre-decomposed by priority, not as a
single monolithic planning unit.

**Recommended follow-up (not done in this pass):** if a single `/speckit-plan` bundling all four
subjects becomes unwieldy in practice, consider formally splitting this spec into two spec-kit
features via `/speckit-specify` — e.g. a `004-rubric-core` (US1) and a `004a-delivery-harvest-comparison`
(US2–US4) — so each gets its own `plan.md`/`tasks.md` lifecycle. That is a repo-restructuring decision
(new directories, a fresh `/speckit-specify` pass, re-deriving cross-references) left to the user to
decide when it's actually needed, not executed here.

### Out of Scope (owned by other specs)

- **Pipeline orchestration / gates / state machine / resume / the delivery terminal-state
  mechanics** — spec **001**. 001 advances phases, enforces gates, and invokes `/course-report` at
  the delivery terminal state; 004 owns the report's **grading and contents**, not when delivery runs
  or the folder contract.
- **Research + `SOURCES.md` population + syllabus composition** — spec **002**. The evaluator *reads*
  `SOURCES.md` and 002's mentor-added / thin-grounding tags for traceability; it does not search,
  weigh, or populate them.
- **Skeleton & lesson authoring, the parallel author–critic worker pool, the per-lesson author-blind
  evaluators, and the fake-student check** — spec **003**. 003 *consumes* 004's rubric as the lesson
  gate and applies it per lesson; 004 defines the rubric and does course-level grading, not authoring.
- **Populating a course's `FEEDBACK.md` during a build** — critiques land there as the build runs;
  004 owns **harvesting** `FEEDBACK.md` up into `insights/`, not the per-critique writing.
- **`/improve-course` backlog execution** — 004's `comparison/` *feeds* that backlog with per-course
  reports; acting on the backlog is the template's `/improve-course` command, not this spec.
- **The pedagogy technique library (`pedagogy/`) as a rubric method-fit add-on input** — a deferred,
  optional candidate for the topic-add-on layer already generic in (1) above; not required for the
  rubric core. See `course-factory/pedagogy/README.md`.
- **An analyze-style cross-artifact consistency check** (brief ↔ syllabus ↔ skeletons ↔ lessons) —
  a deferred sibling to the course-evaluator's independent course-level verdict, catching drift the
  per-phase gates miss.

  Both tracked in `course-factory/DESIGN.md` § Deferred extensions.

## Clarifications

### Session 2026-07-08

- Q: What does "pass the rubric" mean quantitatively (FR-004)? → A: **Per-dimension threshold** —
  every generic-core dimension and every requested topic add-on must clear its own minimum bar; a
  lesson or course passes only if **no** dimension is below threshold. Per-dimension scores are still
  retained for the scorecard.
- Q: Is the course-level verdict independent grading or just a roll-up of per-lesson passes (FR-009)?
  → A: **Independent course-level grading** — the evaluator additionally grades whole-arc dimensions
  (coverage across the syllabus, flow/continuity, the running-example thread) and MAY return a "needs
  work" verdict even when every lesson passed; such gaps feed the `/improve-course` backlog as a
  forward diff, never a phase re-open.
- Q: When does the automatic `FEEDBACK.md` → `insights/` harvest run (FR-016)? → A: **User-invoked
  only** — there is **no** automatic/scheduled harvest; both the per-insight capture and the
  `setup-retro`-style bulk harvest run only when the user invokes them, keeping 004 decoupled from
  001's phase transitions.

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Define the one-rubric-two-layers as the single quality contract (Priority: P1)

The factory has **exactly one** rubric that defines quality: a generic core (correctness, grounding,
flow, coverage, practicality) that always applies, plus optional topic add-ons the course spec can
request (e.g., a fabricated-capacity-numbers check). This rubric is the sole definition of quality —
no rival rubric exists anywhere — and it is expressed so that spec 003 can consume it as the lesson
gate and the course-evaluator can run on it as the engine.

**Why this priority**: The rubric is the foundational contract for the whole quality system and a
hard prerequisite for spec 003 (which consumes it as its gate). Nothing downstream — grading, the
report, comparison — means anything without a single agreed definition of quality. It is
independently valuable: a well-formed, two-layer, single-source rubric is testable on its own. This
independence extends to **planning**: US1 can proceed through `/speckit-plan` and `/speckit-tasks` on
its own, ahead of and without blocking on US2–US4 (see "Plannable increments" under Overview).

**Independent Test**: Inspect the rubric; confirm it has the generic core plus a mechanism for
spec-requested topic add-ons, that it is the only quality definition in the repo (no rival rubric),
and that it exposes a pass/fail gate a lesson evaluator (003) can apply.

**Acceptance Scenarios**:

1. **Given** the factory, **When** the rubric is inspected, **Then** it contains a generic core
   (correctness, grounding, flow, coverage, practicality) that applies to every course.
2. **Given** a course spec requesting a topic add-on, **When** the rubric is assembled for that
   course, **Then** the requested add-on is layered on top of the generic core; when none is
   requested, only the core applies.
3. **Given** the repository, **When** all quality definitions are enumerated, **Then** there is
   exactly one rubric and **no rival rubric** (comparison/ proposes revisions to it, never a second).
4. **Given** spec 003's lesson gate, **When** it grades a lesson, **Then** it consumes this same
   rubric as its pass/fail contract (one shared definition, two consumers).

---

### User Story 2 - Grade a course and emit the graded COURSE_REPORT.md (Priority: P2)

At delivery, the course-evaluator applies the rubric to the finished course, checks that every cited
claim traces to a real `SOURCES.md` entry (tracing, not truth) while honoring 002's mentor-added /
thin-grounding tags, and emits a graded scorecard. The `/course-report` command writes that scorecard
to `COURSE_REPORT.md` — rubric scores, wins, cleanups, and a verdict — reporting the course's quality
to the user, distinct from `FEEDBACK.md`.

**Why this priority**: This is the factory's quality certificate — the artifact that tells the user
"this course meets the bar." It builds on US1's rubric and turns it into a graded, delivered report.
Testable given a finished course and the rubric.

**Independent Test**: Given a finished course and the rubric, run the course-evaluator; confirm it
produces a scorecard with per-dimension scores, wins, cleanups, and a verdict; confirm an unresolvable
`[Sn]` citation is flagged as a traceability finding while a claim explicitly marked mentor-added is
**not** flagged for lacking an `[Sn]`; confirm `COURSE_REPORT.md` is written and reads as a quality
report distinct from `FEEDBACK.md`.

**Acceptance Scenarios**:

1. **Given** a finished course, **When** the course-evaluator runs, **Then** it applies the rubric
   and emits a scorecard with per-dimension scores, wins, cleanups, and a verdict.
2. **Given** a lesson claim citing `[Sn]`, **When** the evaluator checks grounding, **Then** it
   checks that the key resolves to a real `SOURCES.md` entry — verifying **tracing, not truth** —
   and flags an unresolvable citation; this check runs on every citation, not a sample.
3. **Given** a claim explicitly marked **mentor-added** (per 002's compose-and-flag policy), **When**
   the evaluator checks grounding, **Then** it does **not** fail the claim for lacking an `[Sn]`, and
   it preserves the thin-grounding flags rather than re-presenting them as sourced.
4. **Given** the scorecard, **When** `/course-report` runs, **Then** `COURSE_REPORT.md` is written
   with the scores + verdict and reports the course's quality to the user, distinct in purpose from
   `FEEDBACK.md`.
5. **Given** grading reveals a course-level gap after all lessons already passed, **When** it is
   recorded, **Then** it surfaces as a forward diff / `/improve-course` backlog item and does **not**
   re-open a passed phase (honoring 001 FR-023).

---

### User Story 3 - Compound the factory by harvesting FEEDBACK.md into insights/ (Priority: P3)

Each course carries a `FEEDBACK.md` of critiques and things-to-fix. The factory harvests those up
into a cross-course `insights/` digest — via a user-triggered per-insight capture and a user-invoked
`setup-retro`-style bulk harvest (there is no automatic trigger) — so later courses' intake, syllabus
composition, and drafting read a digest that gets richer with every course.

**Why this priority**: This is the compounding mechanism — the factory learning from itself. It is
valuable but not on the critical path to delivering a single course; it pays off across courses.
Testable given a course with a populated `FEEDBACK.md`.

**Independent Test**: Given a course with a populated `FEEDBACK.md`, run the user-invoked harvest;
confirm its critiques are distilled up into `insights/` (append, not clobber), that a user-triggered
capture can also add an insight on demand, that an empty `FEEDBACK.md` harvests as a no-op rather than
an error, and that nothing harvests until the user invokes it (no automatic trigger).

**Acceptance Scenarios**:

1. **Given** a course with a populated `FEEDBACK.md`, **When** the user invokes the harvest, **Then**
   its critiques are distilled up into the cross-course `insights/` digest.
2. **Given** an insight surfaces mid-work, **When** the user triggers a capture, **Then** it is
   recorded into `insights/` on demand, independent of any bulk harvest.
3. **Given** several courses, **When** each is harvested, **Then** `insights/` accumulates
   append-only — no course's harvest clobbers another's.
4. **Given** `insights/` is knowledge (not tooling), **When** intake / syllabus / drafting run, **Then**
   they read the digest (this spec produces and maintains it; the reading belongs to 001/002/003).

---

### User Story 4 - Propose rubric revisions from external courses via comparison/ (Priority: P4)

The factory analyzes well-made external GitHub courses against ours and produces two things: proposed
**revisions to the one rubric** (never a rival rubric) and a **per-course report** feeding the
`/improve-course` backlog. Adopting a proposed revision is a deliberate, reviewed step — the rubric
is never silently mutated or duplicated.

**Why this priority**: This keeps the single quality bar improving over time from real external
examples. It is the lowest-frequency, highest-latency subject and depends on US1's rubric existing.
Testable given an external course to compare against.

**Independent Test**: Given a well-made external course, run `comparison/`; confirm it emits proposed
revisions targeting the **one** rubric (not a second rubric) and a per-course report for the
`/improve-course` backlog; confirm no rival rubric is created and the live rubric is unchanged until a
revision is deliberately adopted.

**Acceptance Scenarios**:

1. **Given** a well-made external course, **When** `comparison/` analyzes it against ours, **Then** it
   produces proposed **revisions to the single rubric** and a per-course report for `/improve-course`.
2. **Given** a proposed revision, **When** it is produced, **Then** it targets the existing rubric and
   **no rival/second rubric** is created or kept.
3. **Given** a proposed revision, **When** it has not been adopted, **Then** grading still uses the
   current adopted rubric version — proposals do not auto-mutate the live rubric.

---

### Edge Cases

- **A claim cites an unresolvable `[Sn]`** (dead/removed source) — the evaluator records a
  traceability finding in the scorecard; at delivery this feeds the report / `/improve-course`, it
  does not re-open a passed phase (001 FR-023).
- **A claim is marked mentor-added with no `[Sn]`** — this is **not** a traceability failure; the
  evaluator honors 002's compose-and-flag policy and preserves the mentor-added tag.
- **A course-level dimension (coverage/flow across the arc) is weak even though every lesson passed** —
  the evaluator independently grades it (FR-009) and MAY return a "needs work" verdict; the gap
  surfaces as a forward diff / `/improve-course` backlog item, never a phase re-open.
- **`comparison/` finds a strong improvement** — it proposes a revision to the one rubric; it never
  silently mutates the live rubric nor keeps a rival.
- **`FEEDBACK.md` is empty at harvest** — the harvest is a no-op, not an error.
- **A rubric revision is proposed but not yet adopted** — grading uses the current adopted rubric
  version, not the proposal.
- **The `insights/` digest is empty early in the factory's life** — a valid state; the harvest appends
  to it and downstream readers treat an empty digest as valid input.
- **A course requests a topic add-on the rubric has no definition for** — the evaluator surfaces the
  unknown add-on rather than silently grading only the core.

## Requirements *(mandatory)*

### Functional Requirements

**The rubric — the single definition of quality**

- **FR-001**: The factory MUST define quality with **exactly one** rubric: the generic core **named
  and defined by 000 FR-013** (five dimensions: Technical Correctness, Grounding/No-Fabrication,
  Pedagogical Flow, Coverage, Practicality — 000 owns that canonical list; this spec does not
  re-derive it) plus **optional topic add-ons** the course spec can request. It is the **sole**
  definition of quality; no rival rubric may exist anywhere. 004 owns the rubric's **grading
  semantics** (weights, per-dimension thresholds, the hard-gate rule); 000 owns the **shape and
  core-dimension list** as the template asset.
- **FR-002**: The generic core MUST apply to **every** course; a topic add-on MUST apply **only** when
  the course spec / `COURSE_BRIEF.md` requests it. An unknown/unavailable requested add-on MUST be
  surfaced, not silently dropped.
- **FR-003**: The rubric MUST be expressed as a **pass/fail gate contract** that spec 003 consumes as
  the lesson gate — one shared definition with two consumers (003's per-lesson gate; 004's
  course-evaluator engine). 004 owns the definition; it MUST NOT re-implement 003's per-lesson
  application.
- **FR-004**: A pass MUST be defined as a **per-dimension threshold**: every generic-core dimension
  **and** every requested topic add-on MUST clear its own minimum bar; a lesson or course passes only
  if **no** dimension falls below its threshold (a strong dimension MUST NOT mask a failing one).
  Per-dimension scores MUST still be retained for the scorecard even though the gate is derived from
  the thresholds. This is the pass contract spec 003 consumes per lesson. **The concrete scale (e.g.,
  1–5) and each dimension's minimum-bar value are owned by this spec's `/speckit-plan`**, not fixed
  here, and MUST be recorded alongside the rubric version (FR-005) so a course's pass/fail is
  reproducible against the exact thresholds that graded it.
- **FR-005**: The rubric MUST be **versioned**, so a course records which rubric version graded it and
  `comparison/` can propose revisions against a known baseline; revisions extend the **single** rubric
  and never create a second. **The rubric's version identity IS the template's version stamp** (000
  FR-016) — the factory maintains **one** version identity across the template and its rubric asset,
  never an independent rubric-only counter. Adopting a proposed revision re-stamps the template via
  000's narrower rubric-only revision path (000 Edge Cases: "a rubric revision proposed by
  `comparison/` is adopted after v1"), not a full re-distillation.

**Course-evaluator**

- **FR-006**: The course-evaluator MUST apply the rubric to a finished course and emit a **graded
  scorecard**: per-dimension scores, wins, cleanups, and a verdict.
- **FR-007**: The evaluator MUST check **citation traceability for every `[Sn]` key in the course** —
  that each resolves to a real `SOURCES.md` entry — verifying **tracing, not truth**, and MUST flag
  any unresolvable citation. Resolution is a cheap, mechanical lookup, unlike verifying truth, so the
  check is exhaustive, not sampled (mirrors 003 FR-011 — this is the factory's **canonical**
  citation/grounding contract; 002 and 003 apply it at their own granularity rather than re-deriving
  it).
- **FR-008**: The evaluator MUST honor 002's **compose-and-flag** thin-grounding policy: a claim
  explicitly marked **mentor-added** MUST NOT be failed merely for lacking an `[Sn]`, and
  thin-grounding flags MUST be preserved, never re-presented as sourced.
- **FR-009**: The evaluator MUST **independently grade course-level dimensions** — coverage across the
  whole syllabus, flow / continuity across the arc, and the running-example thread — and MAY return a
  "needs work" / failing verdict **even when every lesson already passed** its per-lesson gate; the
  course verdict is NOT a mere aggregate of per-lesson passes. Any course-level gap so found MUST feed
  the forward-diff / `/improve-course` backlog (FR-010), never re-open a passed phase.
- **FR-010**: When grading surfaces a gap in an already-passed artifact, the evaluator MUST record it
  as a **forward diff / `/improve-course` backlog item** and MUST NOT re-open or re-flow a passed
  phase (honoring 001 FR-023).

**COURSE_REPORT.md — quality report**

- **FR-011**: The `/course-report` command MUST generate **`COURSE_REPORT.md`** from the evaluator's
  scorecard, holding rubric scores, wins, cleanups, and the verdict, and MUST **report the course's
  graded quality to the user**. This report MAY carry a "needs work" verdict (FR-009) — it is
  delivered as-is in that case, never withheld or blocked, and the gap feeds the `/improve-course`
  backlog (FR-010) rather than gating delivery (001 FR-011/FR-021). 004 owns the report's generation
  and contents; 001 owns invoking it at delivery and treats the report's **generation**, not its
  verdict, as satisfying the delivery gate.
- **FR-012**: `COURSE_REPORT.md` MUST be **distinct in purpose** from `FEEDBACK.md`: the report tells
  the user the course's **graded quality** (pass or needs-work); `FEEDBACK.md` feeds improvement back
  **into the factory**. The two MUST NOT be merged.
- **FR-013**: The report MUST record the **rubric version** used to grade the course (per FR-005), so
  its grading is reproducible and re-syncable.

**FEEDBACK.md → insights/ harvest**

- **FR-014**: The factory MUST **harvest** each course's `FEEDBACK.md` up into the cross-course
  `insights/` digest so later courses benefit — the compounding mechanism.
- **FR-015**: The harvest MUST support **two user-invoked paths**: a **user-triggered per-insight
  capture** (log a single insight on demand) and a **user-invoked `setup-retro`-style bulk harvest**
  of a course's `FEEDBACK.md` into `insights/`.
- **FR-016**: The harvest MUST NOT run on an **automatic or scheduled trigger** — insights compound
  **only when the user invokes** one of the two paths (FR-015). This keeps 004 decoupled from 001's
  phase transitions and delivery mechanics.
- **FR-017**: The harvest MUST be **append-only** into `insights/` — one course's harvest MUST NOT
  clobber another's — and an **empty `FEEDBACK.md`** MUST harvest as a no-op, not an error. `insights/`
  is knowledge, not tooling; producing/maintaining it is 004's, while reading the digest belongs to
  intake (001), syllabus (002), and drafting (003).

**comparison/**

- **FR-018**: `comparison/` MUST analyze well-made external courses against ours and produce **(a)**
  proposed **revisions to the one rubric** and **(b)** a **per-course report** feeding the
  `/improve-course` backlog.
- **FR-019**: `comparison/` MUST NOT keep a **rival rubric**; it only proposes revisions to the single
  rubric, and a proposed revision MUST NOT auto-mutate the live rubric — adoption is a deliberate,
  reviewed step, and grading uses the current adopted version until then. **Minimal adoption
  protocol**: the factory maintainer reviews a proposed revision against real course evidence and
  explicitly approves or rejects it; an approved revision is logged as a **provenance record** in the
  template (000's provenance mechanism, 000 FR-019) citing the `comparison/` proposal, and its
  adoption re-stamps the template's version (000's rubric-only revision path, 000 Edge Cases) — the
  stamp bump **is** the adoption record.

### Key Entities

- **Rubric** — the single definition of quality: a generic core (correctness, grounding, flow,
  coverage, practicality) plus optional spec-requested topic add-ons; versioned; no rival permitted.
- **Generic core** — the always-applied rubric dimensions.
- **Topic add-on** — an optional extra check a course spec requests (e.g., fabricated-capacity-numbers),
  layered on the core.
- **Pass/fail gate contract** — the form of the rubric that spec 003 consumes per-lesson and the
  course-evaluator runs course-wide.
- **Course-evaluator** — the course-level grader that applies the rubric, checks every citation's
  traceability, honors mentor-added tags, and emits the scorecard.
- **Scorecard** — per-dimension scores + wins + cleanups + verdict; the evaluator's output.
- **Verdict** — the course-level pass/needs-work judgment recorded in the scorecard and report.
- **Traceability check** — verification that **every** `[Sn]` claim in the course resolves to a real
  source (tracing, not truth) — exhaustive, not a spot-check, since resolution is a cheap mechanical
  lookup.
- **Mentor-added tag / thin-grounding flag** — 002's markings the evaluator honors and preserves.
- **COURSE_REPORT.md** — the delivered quality report (scores + verdict + rubric version), generated by
  `/course-report`; distinct from `FEEDBACK.md`. Its presence, not a passing verdict, satisfies 001's
  delivery gate (001 FR-011).
- **FEEDBACK.md** — the per-course critique file harvested up into `insights/`.
- **insights/ digest** — the append-only cross-course knowledge store the factory compounds into and
  reads from. **Form**: a small set of topic-organized `.md` files under `insights/` (e.g. one per
  recurring theme — anti-fabrication, running-example design, estimation method, kata design,
  mirroring the categories `course-factory/DESIGN.md` already names), each entry dated and sourced to
  the course it was harvested from; readers (001/002/003) load the whole directory as one digest.
  Exact file layout is a `/speckit-plan`-level detail; this spec fixes only that it is knowledge-only
  markdown, append-only, and directory-shaped (not one monolithic file, to keep individual harvests
  small diffs). Starts **empty** — no pre-existing corpus is seeded from the reference course.
- **Harvest** — the user-invoked mechanism (per-insight capture + `setup-retro`-style bulk harvest;
  no automatic trigger) that pumps `FEEDBACK.md` into `insights/`.
- **comparison/** — the external-course analysis that proposes rubric revisions and per-course
  `/improve-course` reports.
- **Rubric version** — the identifier recording which rubric graded a course and which baseline a
  revision targets.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: There is **exactly one** rubric defining quality across the factory — **0** rival
  rubrics at any time (verifiable by enumeration).
- **SC-002**: The generic core applies to **100%** of graded courses, and a topic add-on is applied
  **only** to courses whose spec requested it — **0** unrequested add-ons and **0** silently dropped
  requested add-ons.
- **SC-003**: **100%** of graded courses produce a scorecard with per-dimension scores, wins,
  cleanups, and a verdict.
- **SC-004**: The evaluator flags **100%** of unresolvable `[Sn]` citations and fails a claim for
  missing grounding **0** times when it is explicitly marked mentor-added (honoring 002).
- **SC-005**: **100%** of delivered courses have a `COURSE_REPORT.md` that records the rubric scores,
  the verdict, and the rubric version, and is distinct from `FEEDBACK.md`.
- **SC-006**: Grading re-opens a passed, gated phase **0** times; every course-level gap found at
  grading is recorded as a forward diff / backlog item (consistent with 001 FR-023).
- **SC-007**: When the user invokes the harvest, **every critique** in a non-empty `FEEDBACK.md` is
  **represented** in `insights/` (distilled, not necessarily verbatim — **0** critiques silently
  dropped), append-only (**0** clobbered prior insights, **0** errors on an empty `FEEDBACK.md`); the
  harvest fires on **0** automatic/scheduled triggers.
- **SC-008**: **100%** of `comparison/` outputs target the single rubric (proposed revisions) — **0**
  rival rubrics created — and the live rubric is unchanged until a revision is deliberately adopted.
- **SC-009**: The same rubric grades both the lesson gate (003) and the course-evaluator (004) in
  **100%** of courses — **0** cases of two different quality definitions in play.
- **SC-010**: A course or lesson passes only when **every** rubric dimension clears its threshold —
  **0** passes with any below-threshold dimension (no aggregate masking).
- **SC-011**: The course-evaluator emits an **independent** course-level verdict able to flag "needs
  work" even when all lessons passed — verifiable by an all-lessons-passing course with incomplete arc
  coverage yielding a flagged verdict.

## Assumptions

- **The rubric is a foundational asset distilled into `course-template/`** (DESIGN roadmap task #1);
  004 owns its **definition and contents**, and its core must land before 003 can consume it (per the
  seam log and recommended build order).
- **Delivery timing and the folder contract are 001's** — 001 reaches the terminal state and invokes
  `/course-report`; 004 owns the report's grading and contents, not when delivery runs.
- **`SOURCES.md` and 002's mentor-added / thin-grounding tags exist** (produced by 002); the evaluator
  reads them for traceability and does not populate them.
- **Spec 003 applies the rubric per lesson**; 004 defines the rubric and does course-level grading —
  the per-lesson author-blind evaluators are 003's.
- **Topic add-ons are requested via the course spec / `COURSE_BRIEF.md`**; the evaluator assembles the
  per-course rubric as core + requested add-ons.
- **`comparison/` runs on demand / manually** (not once per build) and its proposed rubric revisions
  are **human-reviewed** before adoption; it is lineage/improvement tooling, not part of the per-course
  critical path.
- **`FEEDBACK.md` is populated during the build** as critiques surface; 004 owns harvesting it, not the
  per-critique writing.
- **`insights/` may be empty early** in the factory's life; the harvest appends to it and downstream
  readers treat an empty digest as valid.
- **`comparison/`'s source-course selection reuses 002's reliability-weighing discipline** (002
  FR-002, Principle II/III) — stars are a green-flag signal, not proof; "well-made" is judged the same
  way 002 judges a source's reliability, not by a separate rubric for candidate selection.
- **`comparison/`'s analysis process reuses 002's research method** (research → weigh reliability →
  cite → converge-or-budget, also mirrored in `pedagogy/`'s build) rather than inventing a third
  research discipline — it is not a new method, just a different corpus (external courses instead of
  a single topic's sources).
